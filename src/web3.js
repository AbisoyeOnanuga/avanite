// import the alchemy-sdk module
const alchemy = require("alchemy-sdk");

// create an alchemy instance with your Alchemy API key
/*const alch = alchemy.createAlchemy(process.env.REACT_APP_ALCHEMY_KEY);

// use the alchemy instance to access alchemy-web3 and alchemy-sdk features and methods
// for example, get the block number using alchemy-web3
alch.core.getBlockNumber().then((blockNumber) => {
  console.log(blockNumber);
});

// for example, get the NFT metadata using alchemy-sdk
alch.nft.getNftMetadata("0xb47e3cd837dDF8e4c57F05d70Ab865de6e193BBB", 9775).then((nft) => {
  console.log(nft);
});*/
